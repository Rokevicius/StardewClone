using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class Builder : MonoBehaviour
{
    // BuildPlayerOptions buildPlayerOptions = new BuildPlayerOptions();

    // [MenuItem("Build/Make Build for iOS")]
    // private void BuildiOS()
    // {
    //     buildPlayerOptions.target = BuildTarget.iOS;
    //     PlayerSettings.SetScriptingBackend(BuildTargetGroup.iOS, ScriptingImplementation.Mono2x);
    //     BuildPipeline.BuildPlayer(buildPlayerOptions);

    // }

    // [MenuItem("Build/Make Build for Android")]
    //     private static void BuildAndroid()
    // {
    //     buildPlayerOptions.target = BuildTarget.Android;
    //     PlayerSettings.SetScriptingBackend(BuildTargetGroup.Android, ScriptingImplementation.Mono2x);
    //     BuildPipeline.BuildPlayer(buildPlayerOptions);

    // }

    // [MenuItem("Build/Make Build for iOS and Android")]
    //     private void BuildMobile()
    // {
    //     BuildAndroid();
    //     BuildiOS();
    // }
}
